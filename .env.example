# Database Configuration
# Option 1: Use DATABASE_URL
DATABASE_URL=postgresql://username:password@localhost:5432/techstackr_development

# Option 2: Use individual variables (if DATABASE_URL not set)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres

# OpenAI API Configuration
OPENAI_API_KEY=your_openai_api_key_here

# Rails Configuration
RAILS_ENV=development
RAILS_MAX_THREADS=5

# Optional: Redis (if using Action Cable or caching)
# REDIS_URL=redis://localhost:6379/0

# Optional: Supabase (if using)
# SUPABASE_ACCESS_TOKEN=your_token_here
# SUPABASE_PROJECT_REF=your_project_ref

# Security
# Generate with: rails secret
# RAILS_MASTER_KEY=your_master_key_here
